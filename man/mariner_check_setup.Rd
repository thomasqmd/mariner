% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preflight.R
\name{mariner_check_setup}
\alias{mariner_check_setup}
\title{Check that this machine can render a mariner report}
\usage{
mariner_check_setup(
  root = NULL,
  theme = mariner_themes,
  template = "simple_report",
  quiet = FALSE
)
}
\arguments{
\item{root}{Project root to check. \code{NULL} (the default) resolves it with
\code{\link[=mariner_project_root]{mariner_project_root()}}.}

\item{theme}{One of \link{mariner_themes}.}

\item{template}{Starter template whose package dependencies to check.}

\item{quiet}{Return the data frame without printing it.}
}
\value{
Invisibly, a data frame with one row per check and the columns
\code{check}, \code{status} (\code{"ok"}, \code{"warn"} or \code{"fail"}), \code{detail} and \code{remedy}.
}
\description{
Runs through everything a branded PDF needs -- Quarto, a LaTeX engine, the
fonts, the project folders, the theme, the packages the template loads -- and
prints one line per check with a copy-pasteable fix for anything that is
wrong.
}
\details{
Nothing is installed or changed. Every remedy is printed for you to run.
}
\section{What the statuses mean}{

\describe{
\item{ok}{Nothing to do.}
\item{warn}{The report will render, but not as intended -- most often
figures drawn in the device's default typeface while the page text is in
the theme's, which is the failure nobody notices.}
\item{fail}{The render will not complete.}
}
}

\examples{
\dontrun{
mariner_check_setup()

# Just the problems:
report <- mariner_check_setup(quiet = TRUE)
subset(report, status != "ok")
}
}
\seealso{
\code{\link[=mariner_setup_project]{mariner_setup_project()}}, \code{\link[=mariner_install_fonts]{mariner_install_fonts()}}
}
