% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggtheme.R
\name{theme_mariner}
\alias{theme_mariner}
\title{Branded ggplot2 theme for mariner}
\usage{
theme_mariner(
  theme = mariner_themes,
  format = mariner_formats,
  base_size = 11,
  base_family = NULL,
  ...
)
}
\arguments{
\item{theme}{Theme name. Defaults to the built-in mariner theme.}

\item{format}{One of \link{mariner_formats}.}

\item{base_size}{Base font size in points. Defaults to \code{11}, the report body
size set in \verb{_extension.yml}.}

\item{base_family}{Font family. Defaults to \code{"Atkinson Hyperlegible Next"}.}

\item{...}{Further arguments for \code{\link[ggplot2:theme]{ggplot2::theme()}}.}
}
\value{
A ggplot2 theme object.
}
\description{
A ggplot2 theme that matches the page: the brand typeface, background, ink
and grid rules.
}
\examples{
library(ggplot2)
# base_family = "" keeps the device's own font. The bundled families reach a
# plot through systemfonts, which the base pdf() device does not read. A
# report renders through cairo_pdf and gets the real face.
ggplot(mpg, aes(displ, hwy, colour = class)) +
  geom_point() +
  scale_colour_mariner_d() +
  theme_mariner(base_family = "")
}
