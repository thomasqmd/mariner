% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{mariner_setup_project}
\alias{mariner_setup_project}
\title{Set up a mariner project}
\usage{
mariner_setup_project(
  root = NULL,
  theme = mariner_themes,
  template = "report",
  author = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{root}{Project root. \code{NULL} (the default) resolves it with
\code{\link[=mariner_project_root]{mariner_project_root()}}, which is the working directory unless that is
one of the mariner folders. Pass a path to override.}

\item{theme}{One of \link{mariner_themes}.}

\item{template}{Starter template to copy into \verb{reports/}. \code{NULL} for no
starter document.}

\item{author}{Name for the project's author, written to \verb{_mariner.yml}. See
the section below. \code{NULL} (the default) writes no file and leaves any
existing setting alone. A name here is written whether or not \code{overwrite}
is set: \code{overwrite} guards the files setup scaffolds and a student then
edits, and \code{author} is itself the instruction to change this one.}

\item{overwrite}{Replace files that already exist. The folders themselves are
never removed.}
}
\value{
The named list from \code{\link[=mariner_dirs]{mariner_dirs()}}, invisibly.
}
\description{
Creates the folders a mariner workflow uses, installs the Quarto theme into
them, drops in a starter report, and records the project's author. Run it
twice and nothing changes: a file that exists is left alone unless
\code{overwrite = TRUE}.
}
\details{
Beneath \code{root}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{_mariner.yml                    project settings, when author is given
assets/
  _extensions/mariner/          the theme, assembled once
reports/
  _extensions/mariner/          a copy, beside the documents that use it
  report.qmd                    a starter document
zip_files/                      the bundles you hand out
}\if{html}{\out{</div>}}

\verb{zip_files/}, \verb{assets/_extensions/} and \verb{reports/_extensions/} go into the
project \code{.gitignore}. All three are build outputs, and a committed copy goes
stale against what it was built from. \verb{_mariner.yml} is not ignored — it is
a setting, and it belongs with the project.
}
\section{The author}{

\code{author} is a project setting, not a column of \code{\link[=generate_reports]{generate_reports()}}'s
\code{params_df}. One person runs a batch, and it is their name on every report
in it. As a column it had to be retyped on every call, and a call that
omitted it produced a batch of PDFs that carried the template's placeholder
on every title page. The render succeeded, so nothing said so.

The name goes to \verb{_mariner.yml}, and \code{\link[=generate_reports]{generate_reports()}} fills any template
parameter that matches it. A \code{params_df} column still wins, for the batch
whose author does vary.
}

\examples{
\dontrun{
# In an RStudio project or Quarto project directory:
mariner_setup_project(author = "Alice Smith")

# Or somewhere explicit:
mariner_setup_project(root = "~/classes/stat3010", author = "Alice Smith")

# Change the author later. This touches nothing else:
mariner_setup_project(author = "A. Smith", template = NULL)
}
}
\seealso{
\code{\link[=mariner_dirs]{mariner_dirs()}}, \code{\link[=mariner_install_fonts]{mariner_install_fonts()}}
}
